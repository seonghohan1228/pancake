# stb_image_write

`stb_image_write.h` from https://github.com/nothings/stb (public domain /
MIT dual license, see the header itself). Used by the GUI to export plot
screenshots as PNG. Compiled into the exe; nothing read at runtime.
